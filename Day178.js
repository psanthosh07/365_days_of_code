// DO NOT USE ARGUMENTS FOR INPUTS
// Input num valus is in variable *num*
var N = parseInt(readLine().trim());
// YOUR CODE GOES HERE
var log2Value = Math.round(Math.log2(N));

console.log(log2Value);
